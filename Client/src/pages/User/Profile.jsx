import React from 'react';
import UserDetails from '../../components/User-UI/UserDetails';
import Layout from '../../components/User-UI/Layout';

const Profile = () => {
  return (
    <Layout>
      <UserDetails />
    </Layout>
  );
};

export default Profile;
